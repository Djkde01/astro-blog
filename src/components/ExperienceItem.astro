---
import Link from "./Link.astro";
interface Props {
  title: string;
  description: string;
  period: string;
  link?: string;
}

const { title, description, period, link } = Astro.props as Props;
---

<div class="timeline" aria-hidden="true"></div>
<time>{period}</time>
<h3 class="h3">{title}</h3>
<p>
  {description}
</p>
{
  link && (
    <Link text="Saber más" href={link} style="secondary" target="_blank" />
  )
}
