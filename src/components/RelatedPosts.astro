---
import { slugify, formatDate } from "../js/utils";

const { relatedPosts } = Astro.props;
---

{
  relatedPosts.map((post) => (
    <div class="post">
      <h4 class="h4">
        <a href={post.url}>{post.frontmatter.title}</a>
      </h4>
      <small>
        <small>
          <a href={`/about`}>Sergio Estrella</a> -{" "}
          {formatDate(post.frontmatter.date)}
        </small>
      </small>
    </div>
  ))
}
